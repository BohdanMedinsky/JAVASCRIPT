// функція скорочення рядка. 
   function truncate(str, maxlength) {
        console.log(`5)\n`);
        if (str.length==0)
            console.log(`введено пустий рядок`);
        else
            return (str.length > maxlength) ? str.slice(0, maxlength - 1) + '…' : str;
            console.log(`${myFunctions.truncate(str1,maxlength)}`);
        }
    

        console.log(`${myFunctions.truncate(str,maxlength)}`);














